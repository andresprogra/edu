<%#if user_signed_in? %>
    <%#if current_user.name == nil or current_user.matricula ==nil %>
        <!--<br></br>
        <br></br>-->
    <%#else%>
        <header>
    <div class="bs-component">
          <div class="navbar navbar-default">
            <div class="container-fluid">
              <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                  <span class="icon-bar"></span>
                </button>
                <!--<a class="navbar-brand" href="javascript:void(0)">Mi plataforma</a>-->
                <%name="Mi plataforma"%>
                <%= link_to name, root_path, class:"navbar-brand"%>
              </div>
              <div class="navbar-collapse collapse navbar-responsive-collapse">
                <%if user_signed_in?%>
                  <ul class="nav navbar-nav">
                    <li class="dropdown">
                      <a href="#" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Avisos
                      <b class="caret"></b></a>
                      <ul class="dropdown-menu">
                          <li>
                            <%= link_to "Ver avisos", ads_path%>
                          </li>
                          <li>
                            <%=link_to "Crear avisos", new_ad_path%>
                          </li>
                          <li>
                            <%=link_to "Avisos vencidos", ads_vencidos_path%>
                          </li>
                      </ul>
                    </li>
                    <li><a href="javascript:void(0)">Link</a></li>
                    <li class="dropdown">
                      <a href="#" data-target="#" class="dropdown-toggle" data-toggle="dropdown">Dropdown
                        <b class="caret"></b></a>
                      <ul class="dropdown-menu">
                        <li><a href="javascript:void(0)">Another action</a></li>
                        <li><a href="javascript:void(0)">Something else here</a></li>
                        <li class="divider"></li>
                        <li class="dropdown-header">Dropdown header</li>
                        <li><a href="javascript:void(0)">Separated link</a></li>
                        <li><a href="javascript:void(0)">One more separated link</a></li>
                      </ul>
                    </li>
                  </ul>
                <%end%>
                <ul class="nav navbar-nav navbar-right">
                  <% if user_signed_in? %>
                    
                      <li class="nav nav-pills active"><a href="javascript:void(0)">Puntos: <span class="badge"><%=current_user.puntos%></span></a></li>

                    <% if current_user.name != nil %>
                      <li class="dropdown">
                        <a href="#" data-target="#" class="dropdown-toggle" data-toggle="dropdown"><%if current_user.name != nil%><%=current_user.name%><%end%>
                        <b class="caret"></b></a>
                        <ul class="dropdown-menu">
                          <li>
                            <a href="#">
                            <%if current_user.name != nil%><%=current_user.name%> <%=current_user.apellido%><%end%>
                            </a>
                          </li>
                          <li>
                            <a href="#">
                            <%if current_user.semestre != nil && current_user.grupo != nil%><%=current_user.semestre%> <%= current_user.grupo%><%end%>
                            </a>
                          </li>
                          <li>
                            <a href="#">
                            <%if current_user.carrera != nil%><%=current_user.carrera%><%end%>
                            </a>
                          </li>
                          <li class="divider"></li>
                          <li class="dropdown-header">Configuración</li>
                          <%if user_signed_in? %>
                            <li>
                              <%=link_to "Ver perfil", users_path%>
                            </li>
                            <li>
                              <li><%=link_to "Editar Perfil", edit_user_registration_path%></li>
                            </li>
                            <li>
                              <%= link_to "Cerrar sesión", destroy_user_session_path, method: :delete%>
                            </li>
                          <%end%>
                        </ul>
                      </li>
                    <%end%>
                  <%end %>
                  <%unless user_signed_in? %>
                    <li>
                      <%= link_to "Iniciar sesión", new_user_session_path%>
                    </li>
                    <li>
                      <%= link_to "Registrarte", new_user_registration_path %>
                    </li>
                  <%end%> 
                  
                </ul>
              </div>
            </div>
          </div>
        <div id="source-button" class="btn btn-primary btn-xs" style="display: none;">&lt; &gt;</div></div>
  </header>
    <%#end%>
<%#end%>
