<div class="container">
  <h3>Avisos vencidos</h3>
  <% if @count_ads > 0 %>
   <div class="top-space"></div>
    <% @ads.each do |ad| %>
    <div class="panel panel-primary">
     <div class="panel-heading">
       <h3 class="panel-title"><%= link_to ad.title, ad%></h3>
     </div>
     <div class="panel-body">
       <%= ad.description%>
       
     </div>
    </div>
    <%end%>
    <%else%>
    <h2>No hay avisos vencidos</h2>
   <%end%>
</div>
